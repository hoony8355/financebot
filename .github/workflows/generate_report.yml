name: Autonomous Financial Report Generator

on:
  schedule:
    - cron: '0 */3 * * *' # 3시간마다 실행
  workflow_dispatch: # 수동 실행 버튼

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install @google/genai

      - name: Generate AI Report
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: node scripts/generate.mjs

      - name: Commit and Push Changes
        run: |
          git config --global user.name "FinanceBot-AI"
          git config --global user.email "bot@finance-ai.pro"
          
          # 생성된 데이터 파일 스테이징
          git add public/data/reports-manifest.json public/data/articles/*.json
          
          if git diff --staged --quiet; then
            echo "No changes detected"
          else
            # 스테이징되지 않은 나머지(npm install 관련 등) 제거
            git checkout -- .
            
            git commit -m "🚀 Auto-update: New Financial Analysis Report"
            
            # 리베이스를 통해 동시성 충돌 방지
            git pull --rebase --autostash origin main
            
            git push origin main
          fi
