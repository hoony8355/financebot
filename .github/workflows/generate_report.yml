
name: Autonomous Financial Report Generator

on:
  schedule:
    - cron: '0 */3 * * *' # 3시간마다 실행
  workflow_dispatch: # 수동 실행 버튼

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install @google/genai

      - name: Generate AI Report
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: node scripts/generate.mjs

      - name: Commit and Push Changes
        run: |
          git config --global user.name "FinanceBot-AI"
          git config --global user.email "bot@finance-ai.pro"
          
          # 1. 생성된 데이터 파일만 스테이징
          git add data/reports.json
          
          # 2. 변경사항이 있는지 확인
          if git diff --staged --quiet; then
            echo "No changes detected in reports.json"
          else
            # 3. 중요: 스테이징되지 않은 나머지 변경사항(package-lock.json 등)을 모두 제거하여 작업 트리 정리
            git checkout -- .
            
            # 4. 커밋 생성 (Vercel 배포 트리거를 위해 [skip ci] 미포함)
            git commit -m "🚀 Auto-update: New Financial Analysis Report"
            
            # 5. 원격 저장소와 동기화 (리베이스)
            # --autostash는 혹시 모를 잔여 변경사항을 잠시 보관했다가 합쳐줍니다.
            git pull --rebase --autostash origin main
            
            # 6. 최종 푸시
            git push origin main
          fi
